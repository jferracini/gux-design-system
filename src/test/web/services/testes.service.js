(function(angular) {

    'use strict';

    function repoService(http) {
        return {

            // service 

        };
    }

    angular.module('gux.test.services')
        .factory('repoService', ['$http', repoService]);

}(window.angular));